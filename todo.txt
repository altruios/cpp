// make linux/windows/mac agnostic
// split render and display into procs or thread
// pipe data to ffplay directly through an api?
// output agnostic stream data - useable by vlc/youtube (via localhost?) idk: will explore?
